# Hub configuration — public demo URLs for each tool.
# These point the "Launch interactive demo" buttons at each tool's demo
# subdomain. Set them in Vercel (Project: demoland) once DNS is assigned.

NEXT_PUBLIC_TUESDAY_DEMO_URL=https://tuesday.try.example.com
NEXT_PUBLIC_SYSTEMREADY_DEMO_URL=https://systemready.try.example.com
NEXT_PUBLIC_DOCKTRAIL_DEMO_URL=https://docktrail.try.example.com
